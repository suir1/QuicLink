{
  "name": "quiclink-android",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare:web": "npm --prefix ../web run build",
    "cap:add": "cap add android",
    "cap:sync": "npm run prepare:web && cap sync android",
    "cap:open": "cap open android",
    "android:run": "npm run cap:sync && cap run android"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0",
    "typescript": "^5.9.3"
  }
}
